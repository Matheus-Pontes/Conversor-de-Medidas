import styles from '../../styles/Footer.module.css'

export function Footer() {
    return (
        <footer className={styles.Footer}>
            <h4 className={styles.title}>Criado por Matheus Pontes</h4>
        </footer>
    )
}